<div class="caontainer-fluid">
	<nav class="navbar navbar-expand-lg navbar-light bg-light">
		<div class="container">
			<a class="navbar-brand" href="#">Opérator Skills</a>
			<button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
				<span class="navbar-toggler-icon"></span>
			</button>
			<div class="collapse navbar-collapse" id="navbarSupportedContent">
				<ul class="navbar-nav me-auto mb-2 mb-lg-0 d-flex">
					{% if is_granted('ROLE_ADMIN') or is_granted('ROLE_COMMERCIAL') %}
					<li class="nav-item">
						<a class="nav-link" aria-current="page" href="{{ path('dashboard') }}"><i class="far fa-home fa-fw"></i> Dashboard</a>
					</li>
					{% endif %}
					{% if is_granted('ROLE_COLLABORATEUR') or is_granted('ROLE_ADMIN') %}
					<li class="nav-item">
						<a class="nav-link" href="{{ path('profil', {'id': app.user.id }) }}">
							<i class="far fa-user fa-fw"></i>
							Mon Profil</a>
					</li>
					{% endif %}

					{% if is_granted('ROLE_ADMIN') %}
					<li class="nav-item">
						<a class="nav-link" href="{{ path('add_profile') }}">
							<i class="far fa-user-plus fa-fw"></i>
							Ajouter un Profil</a>
					</li>
					{% endif %}
					{% if is_granted('ROLE_COMMERCIAL') or is_granted('ROLE_ADMIN') %}
					<li class="nav-item dropdown">
						<a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
							Paramètres
						</a>
						<ul class="dropdown-menu" aria-labelledby="navbarDropdown">
							<li>
								<a class="dropdown-item" href="{{ path('search') }}">
									<i class="far fa-search fa-fw"></i>
									Rechercher</a>
							</li>
							<li>
								<a class="dropdown-item" href="{{ path('collaborateurs') }}">
									<i class="far fa-users fa-fw"></i>
									Collaborateurs</a>
							</li>
							<li>
								<a class="dropdown-item" href="{{ path('candidats') }}">
									<i class="far fa-handshake fa-fw"></i>
									Candidats</a>
							</li>
							{% if is_granted('ROLE_ADMIN') %}
							<li>
								<a class="dropdown-item" href="{{ path('skills')}}">
									<i class="far fa-user-graduate fa-fw"></i>
									Compétences</a>
							</li>
							<li>
								<a class="dropdown-item" href="{{ path('category')}}">
									<i class="far fa-folder-tree fa-fw"></i>
									Catégories</a>
							</li>
							<li>
								<a class="dropdown-item" href="{{ path('entreprises') }}">
									<i class="far fa-starship fa-fw"></i>
									Entreprises</a>
							</li>
							{% endif %}
						</ul>
					</li>
					{% endif %}
					
				</ul>

				{% if app.user %}
					<div class="nav-item ms-auto">
						<a class="nav-link " href="{{ path('app_logout') }}">Se Déconnecter</a>
					</div>
				{% else %}
					<div class="nav-item ms-auto">
						<a class="nav-link " href="{{ path('app_logout') }}">Inscription</a>
					</div>
					<div class="nav-item">
						<a class="nav-link " href="{{ path('app_login') }}">Se connecter</a>
					</div>
				{% endif %}

			</div>
		</div>
		
	</nav>
</div>

<!-- Recherche -->
{% if is_granted('ROLE_ADMIN') or is_granted('ROLE_COMMERCIAL') %}
<div class="container-fluid bg-white py-2">
	<div class="container">


		{# {{ render(controller(
			'App\\Controller\\SearchController::searchBar'
		)) }}  #}

		{# {{ form_start(searchFormView) }}
			<div class="row">
				<div class="col">{{ form_row(searchFormView.search) }}</div>
				<div class="col-auto"><button class="btn btn-outline-secondary my-2 px-4" type="submit">Rechercher</button></div>
			</div>
		{{ form_end(searchFormView) }} #}

	</div>
</div>
{% endif %}
